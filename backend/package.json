{
  "name": "lukudiplomi-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "preinstall": "cross-env NODE_ENV=development node scripts/createDatabase.js",
    "postinstall": "npx knex migrate:latest",
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "lint": "eslint .",
    "db:create": "node scripts/createDatabase.js",
    "db:make": "npx knex migrate:make --stub migration.stub --",
    "db:seed": "npx knex seed:make --stub seed.stub --"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "express": "^4.16.2",
    "express-rate-limit": "^8.2.1",
    "express-session": "^1.19.0",
    "knex": "^3.1.0",
    "lusca": "^1.7.0",
    "memorystore": "^1.6.7",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "passport-local": "^1.0.0",
    "pg": "^8.18.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@stylistic/eslint-plugin": "^4.4.1",
    "eslint": "^9.39.2",
    "eslint-plugin": "^1.0.1",
    "globals": "^17.3.0",
    "nodemon": "^3.1.11"
  },
  "comments": {
    "type": "module/commonjs",
    "scripts": {
      "pre...": "When running ..., Runs before ... script",
      "post...": "When running ..., Runs after ... script",
      "preinstall": "creates production/development database",
      "postinstall": "Runs migrations for production/development database",
      "prestart": "Checks if frontend build is found, and if not, builds the frontend into frontend/dist/ (doesn't work in Windows command prompt or powershell, use git bash, if you don't have git bash, build frontend manually)",
      "start": "starts server in production environment",
      "dev": "starts server in development environment",
      "lint": "runs linter",
      "db:create": "creates production/development database",
      "db:make": "creates a knex migration template with name specified by user",
      "db:seed": "creates a knex seed template with name specified by user"
    },
    "dependencies": {
      "bcrypt": "Used for hashing passwords, based on the blowfish cipher",
      "cors": "CORS is used to enable cross-origin resource sharing, allowing the backend to handle requests from different origins",
      "cross-env": "Required for NODE_ENV to work on Windows",
      "dotenv": "Environment variable management",
      "express": "Web application framework for Node.js",
      "express-rate-limit": "Used to limit log-in attempts from IP addresses",
      "express-session": "For storing session data server-side (https://expressjs.com/en/resources/middleware/session.html)",
      "knex": "SQL query builder for Node.js",
      "lusca": "Used for creating and comparing CSRF tokens",
      "memorystore": "Used to prevent memory leaks caused by express-session (https://www.npmjs.com/package/memorystore)",
      "passport": "Used for authentication (https://www.npmjs.com/package/passport)",
      "passport-google-oauth20": "Passport plugin for Google OAuth 2.0",
      "passport-local": "Plugins that the passport uses as strategies (https://github.com/jaredhanson/passport-local)",
      "pg": "PostgreSQL client for Node.js",
      "zod": "Used in error handling (input validation). More documentation (https://zod.dev/)"
    },
    "devDependencies": {
      "devDependencies": "packages used only during testing and development",
      "eslint": "ESLint is used to maintain code quality and consistency (automatic testing of code style)",
      "globals": "Globals is used to define global variables for ESLint",
      "nodemon": "Nodemon is used for hot reloading the running server when new changes are saved"
    }
  }
}
